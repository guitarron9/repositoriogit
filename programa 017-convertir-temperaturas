# Convertir temperatura
print('Conversión de temperaturas de centígrados a Fahrenheit ...')
opcion = input("Convertir a [C]entígrados o convertir a [F]ahrenheit: ").upper()
if opcion == 'C':
    print("\nConvirtiendo a Centígrados ...")
    temp = float(input("Grados Fahrenheit? "))
    res = (temp - 32) * 5 / 9
    print(f"{temp} grados Fahrenheit equivalen a {res:.2f} grados Centígrados")
elif opcion == 'F':
    print("\nConvirtiendo a Fahrenheit...")
    temp = float(input("Dame los Centígrados? "))
    res = (temp * 9 / 5) + 32
    print(f"{temp} grados Centígrados equivalen a {res:.2f} grados Fahrenheit")
else:
    print("Escala no válida")