# p28-aceptar-estudiante-v2
import os;

os.system('clear')

# Imprime números de 1 a 200 de 10 en 10
c = 1
while c < 200:
    c += 1
    if c % 10 != 0:
        continue  # Si no es múltiplo de 10, pasa a la siguiente iteración
    print(c, end=" ")
