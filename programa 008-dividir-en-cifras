# dividir un m¿numero entero de3 cifras en unidades, decenas, entenas
# 145

import os; os.system('clear')

print('Dividir un numero entero de 3 cifras en unidades.decenas y centenas')

# numero = int(input('dame un numero entero de 3 cifras ? '))

n= 145
 
u = n % 10
n = n // 10
d = n % 10
n = n // 10
c = n

print(f'centenas: {c}\nDecenas: {d}\n,unidades:{u}')

